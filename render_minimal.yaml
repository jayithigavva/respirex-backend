services:
  - type: web
    name: respirex-api-working
    env: python
    plan: free
    buildCommand: pip install -r requirements_minimal.txt
    startCommand: uvicorn app_minimal:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
